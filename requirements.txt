# EduPro Learner Intelligence Platform
# Streamlit Cloud requirements — at repo root so Cloud auto-detects it.
# Flexible pins to avoid resolver conflicts on Cloud's Python environment.

# -- CORE DATA --
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.10,<2.0

# -- MACHINE LEARNING --
scikit-learn>=1.3,<2.0
joblib>=1.3

# -- VISUALIZATION --
plotly>=5.18

# -- DASHBOARD --
streamlit>=1.30

# -- UTILITIES --
python-dateutil>=2.8
pytz>=2023.3
openpyxl>=3.1
